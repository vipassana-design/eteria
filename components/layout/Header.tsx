/** Header — se construye en la Fase 3 (scroll state, menú mobile).
 *  Por ahora solo reserva el lugar en el layout. */
export default function Header() {
  return <header className="fixed inset-x-0 top-0 z-50 h-20" />
}
